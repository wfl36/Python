#将序列分解为单独的变量

p = (4,5)
x,y = p

data = ['acme',50,18,(2017,9,4)]
#name,age,date = data

#_,age,price,_ = data
#可以选择一个不长用到的变量名，作为丢弃的变量名，保证该变量名没有在其他地方用过

# 如果元素数量不匹配将会报一个错

#x,y,z = p

# 元组或列表，只要对象恰好是可迭代，就可以执行分解

s = 'hello';
a,b,c,d,e = s
#print(e)
